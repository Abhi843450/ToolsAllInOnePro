/**
 * Font Pairing Generator — handler.js
 * Delegates to Processors.font_pairing() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.font_pairing()
})();
