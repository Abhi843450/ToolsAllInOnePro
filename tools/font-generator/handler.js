/**
 * Font Style Generator — handler.js
 * Delegates to Processors.font_generator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.font_generator()
})();
