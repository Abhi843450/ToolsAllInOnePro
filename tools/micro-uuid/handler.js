/**
 * Micro ID Generator — handler.js
 * Delegates to Processors.micro_uuid() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.micro_uuid()
})();
