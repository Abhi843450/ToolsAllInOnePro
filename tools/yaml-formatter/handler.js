/**
 * YAML Formatter — handler.js
 * Delegates to Processors.yaml_formatter() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.yaml_formatter()
})();
