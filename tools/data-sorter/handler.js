/**
 * Data Sorter — handler.js
 * Delegates to Processors.data_sorter() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.data_sorter()
})();
