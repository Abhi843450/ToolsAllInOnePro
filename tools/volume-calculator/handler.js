/**
 * Volume Calculator — handler.js
 * Delegates to Processors.volume_calculator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.volume_calculator()
})();
