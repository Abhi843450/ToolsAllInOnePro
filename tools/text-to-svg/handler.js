/**
 * Text to SVG — handler.js
 * Delegates to Processors.text_to_svg() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.text_to_svg()
})();
