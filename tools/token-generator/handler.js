/**
 * Token Generator — handler.js
 * Delegates to Processors.token_generator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.token_generator()
})();
