/**
 * Unicode Encoding — handler.js
 * Delegates to Processors.unicode_encode() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.unicode_encode()
})();
