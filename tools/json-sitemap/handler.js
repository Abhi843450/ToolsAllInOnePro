/**
 * JSON to Sitemap — handler.js
 * Delegates to Processors.json_sitemap() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.json_sitemap()
})();
