/**
 * Caesar Cipher Brute Force — handler.js
 * Delegates to Processors.brute_force_decoder() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.brute_force_decoder()
})();
