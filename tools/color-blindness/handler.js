/**
 * Color Blindness Simulator — handler.js
 * Delegates to Processors.color_blindness() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.color_blindness()
})();
