/**
 * XML Formatter — handler.js
 * Delegates to Processors.xml_formatter() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.xml_formatter()
})();
