/**
 * CSS Clip Path Generator — handler.js
 * Delegates to Processors.clip_path_generator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.clip_path_generator()
})();
