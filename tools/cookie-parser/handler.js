/**
 * Cookie String Parser — handler.js
 * Delegates to Processors.cookie_parser() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.cookie_parser()
})();
