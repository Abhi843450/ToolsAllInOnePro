/**
 * Probability Calculator — handler.js
 * Delegates to Processors.probability() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.probability()
})();
