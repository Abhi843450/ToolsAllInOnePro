/**
 * Hash Lookup — handler.js
 * Delegates to Processors.hash_lookup() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.hash_lookup()
})();
