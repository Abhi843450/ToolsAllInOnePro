/**
 * Text Encoding Detector — handler.js
 * Delegates to Processors.encoding_detector() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.encoding_detector()
})();
