/**
 * Coin Flip Simulator — handler.js
 * Delegates to Processors.coin_flip() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.coin_flip()
})();
