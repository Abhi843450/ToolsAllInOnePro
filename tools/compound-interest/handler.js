/**
 * Compound Interest Calculator — handler.js
 * Delegates to Processors.compound_interest() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.compound_interest()
})();
