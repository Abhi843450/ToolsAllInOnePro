/**
 * Name Generator — handler.js
 * Delegates to Processors.name_generator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.name_generator()
})();
