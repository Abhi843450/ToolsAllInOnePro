/**
 * Number to Words — handler.js
 * Delegates to Processors.number_to_words() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.number_to_words()
})();
