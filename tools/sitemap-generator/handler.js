/**
 * Sitemap XML Generator — handler.js
 * Delegates to Processors.sitemap_generator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.sitemap_generator()
})();
