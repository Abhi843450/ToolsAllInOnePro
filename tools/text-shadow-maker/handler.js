/**
 * CSS Text Shadow Maker — handler.js
 * Delegates to Processors.text_shadow_maker() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.text_shadow_maker()
})();
