/**
 * Environment Variables to JSON — handler.js
 * Delegates to Processors.env_to_json() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.env_to_json()
})();
