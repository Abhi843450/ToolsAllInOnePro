/**
 * JSX to HTML Converter — handler.js
 * Delegates to Processors.jsx_to_html() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.jsx_to_html()
})();
