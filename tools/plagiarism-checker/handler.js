/**
 * Text Similarity Checker — handler.js
 * Delegates to Processors.plagiarism_checker() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.plagiarism_checker()
})();
