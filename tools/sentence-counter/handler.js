/**
 * Sentence Counter — handler.js
 * Delegates to Processors.sentence_counter() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.sentence_counter()
})();
