/**
 * Password Strength Checker — handler.js
 * Delegates to Processors.password_strength() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.password_strength()
})();
