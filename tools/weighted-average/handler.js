/**
 * Weighted Average Calculator — handler.js
 * Delegates to Processors.weighted_average() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.weighted_average()
})();
