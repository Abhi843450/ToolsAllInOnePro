/**
 * Git Commands Reference — handler.js
 * Delegates to Processors.git_cheatsheet() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.git_cheatsheet()
})();
