/**
 * Percentage Calculator — handler.js
 * Delegates to Processors.percentage_calculator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.percentage_calculator()
})();
