/**
 * Readability Score Calculator — handler.js
 * Delegates to Processors.readability_score() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.readability_score()
})();
