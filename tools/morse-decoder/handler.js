/**
 * Morse Code Decoder — handler.js
 * Delegates to Processors.morse_decoder() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.morse_decoder()
})();
