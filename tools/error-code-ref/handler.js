/**
 * Error Code Reference — handler.js
 * Delegates to Processors.error_code_ref() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.error_code_ref()
})();
