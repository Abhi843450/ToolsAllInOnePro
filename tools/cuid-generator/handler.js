/**
 * CUID Generator — handler.js
 * Delegates to Processors.cuid_generator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.cuid_generator()
})();
