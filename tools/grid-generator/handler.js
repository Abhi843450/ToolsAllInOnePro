/**
 * CSS Grid Generator — handler.js
 * Delegates to Processors.grid_generator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.grid_generator()
})();
