/**
 * Extended ASCII Map — handler.js
 * Delegates to Processors.character_map() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.character_map()
})();
