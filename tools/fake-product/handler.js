/**
 * Fake Product Data Generator — handler.js
 * Delegates to Processors.fake_product() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.fake_product()
})();
