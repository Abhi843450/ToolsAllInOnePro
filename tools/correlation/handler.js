/**
 * Correlation Coefficient Calculator — handler.js
 * Delegates to Processors.correlation() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.correlation()
})();
