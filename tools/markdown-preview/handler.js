/**
 * Markdown Preview — handler.js
 * Delegates to Processors.markdown_preview() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.markdown_preview()
})();
