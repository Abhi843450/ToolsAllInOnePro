/**
 * Speed Converter — handler.js
 * Delegates to Processors.speed_converter() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.speed_converter()
})();
