/**
 * Moving Average Calculator — handler.js
 * Delegates to Processors.moving_average() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.moving_average()
})();
