/**
 * Time Converter — handler.js
 * Delegates to Processors.time_converter() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.time_converter()
})();
