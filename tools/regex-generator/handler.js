/**
 * Regex Generator — handler.js
 * Delegates to Processors.regex_generator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.regex_generator()
})();
