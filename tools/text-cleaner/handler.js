/**
 * Text Cleaner — handler.js
 * Delegates to Processors.text_cleaner() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.text_cleaner()
})();
