/**
 * Line Numberer — handler.js
 * Delegates to Processors.line_numberer() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.line_numberer()
})();
