/**
 * GCD & LCM Calculator — handler.js
 * Delegates to Processors.gcd_lcm() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.gcd_lcm()
})();
