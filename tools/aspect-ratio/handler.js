/**
 * Aspect Ratio Calculator — handler.js
 * Delegates to Processors.aspect_ratio() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.aspect_ratio()
})();
