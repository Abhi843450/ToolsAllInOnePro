/**
 * Hash Generator — handler.js
 * Delegates to Processors.hash_generator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.hash_generator()
})();
