/**
 * Geometric Mean Calculator — handler.js
 * Delegates to Processors.geometric_mean() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.geometric_mean()
})();
