/**
 * Binary ↔ Text Converter — handler.js
 * Delegates to Processors.binary_text() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.binary_text()
})();
