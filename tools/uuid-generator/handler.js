/**
 * UUID Generator — handler.js
 * Delegates to Processors.uuid_generator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.uuid_generator()
})();
