/**
 * Prime Number Checker — handler.js
 * Delegates to Processors.prime_checker() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.prime_checker()
})();
