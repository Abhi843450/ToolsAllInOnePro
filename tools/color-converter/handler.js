/**
 * Color Format Converter — handler.js
 * Delegates to Processors.color_converter() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.color_converter()
})();
