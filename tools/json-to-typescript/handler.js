/**
 * JSON to TypeScript Interface — handler.js
 * Delegates to Processors.json_to_typescript() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.json_to_typescript()
})();
