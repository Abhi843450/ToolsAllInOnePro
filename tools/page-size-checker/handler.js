/**
 * Page Size Calculator — handler.js
 * Delegates to Processors.page_size_checker() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.page_size_checker()
})();
