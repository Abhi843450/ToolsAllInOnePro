/**
 * Font Format Converter — handler.js
 * Delegates to Processors.font_converter() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.font_converter()
})();
