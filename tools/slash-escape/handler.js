/**
 * String Escape Tool — handler.js
 * Delegates to Processors.slash_escape() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.slash_escape()
})();
