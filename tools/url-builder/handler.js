/**
 * URL Builder — handler.js
 * Delegates to Processors.url_builder() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.url_builder()
})();
