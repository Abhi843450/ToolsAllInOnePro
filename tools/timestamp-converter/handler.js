/**
 * Timestamp Converter — handler.js
 * Delegates to Processors.timestamp_converter() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.timestamp_converter()
})();
