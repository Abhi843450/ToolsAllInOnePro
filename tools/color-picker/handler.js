/**
 * Color Picker — handler.js
 * Delegates to Processors.color_picker() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.color_picker()
})();
