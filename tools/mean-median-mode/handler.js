/**
 * Mean, Median & Mode Calculator — handler.js
 * Delegates to Processors.mean_median_mode() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.mean_median_mode()
})();
