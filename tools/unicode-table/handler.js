/**
 * Unicode Table Explorer — handler.js
 * Delegates to Processors.unicode_table() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.unicode_table()
})();
