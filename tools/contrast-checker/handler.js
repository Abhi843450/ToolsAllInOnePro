/**
 * WCAG Contrast Checker — handler.js
 * Delegates to Processors.contrast_checker() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.contrast_checker()
})();
