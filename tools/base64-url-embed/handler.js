/**
 * Base64 URL Embed Generator — handler.js
 * Delegates to Processors.base64_url_embed() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.base64_url_embed()
})();
