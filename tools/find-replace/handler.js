/**
 * Find and Replace — handler.js
 * Delegates to Processors.find_replace() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.find_replace()
})();
