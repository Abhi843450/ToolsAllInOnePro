/**
 * Vigenère Cipher — handler.js
 * Delegates to Processors.vigenere_cipher() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.vigenere_cipher()
})();
