/**
 * Regex Cheat Sheet — handler.js
 * Delegates to Processors.regex_cheatsheet() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.regex_cheatsheet()
})();
