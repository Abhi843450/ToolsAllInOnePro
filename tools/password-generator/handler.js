/**
 * Password Generator — handler.js
 * Delegates to Processors.password_generator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.password_generator()
})();
