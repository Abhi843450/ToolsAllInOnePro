/**
 * Morse Code Converter — handler.js
 * Delegates to Processors.morse_code() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.morse_code()
})();
