/**
 * Currency Converter — handler.js
 * Delegates to Processors.currency_converter() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.currency_converter()
})();
