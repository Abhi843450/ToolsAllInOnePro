/**
 * HTML Formatter — handler.js
 * Delegates to Processors.html_formatter() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.html_formatter()
})();
