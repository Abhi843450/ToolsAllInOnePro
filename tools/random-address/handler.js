/**
 * Random Address Generator — handler.js
 * Delegates to Processors.random_address() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.random_address()
})();
