/**
 * Roman to Arabic Converter — handler.js
 * Delegates to Processors.roman_to_arabic() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.roman_to_arabic()
})();
