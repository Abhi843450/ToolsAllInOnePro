/**
 * Encoding Chain Tool — handler.js
 * Delegates to Processors.encoding_chains() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.encoding_chains()
})();
