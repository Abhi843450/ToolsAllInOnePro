/**
 * Date Calculator — handler.js
 * Delegates to Processors.date_calculator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.date_calculator()
})();
