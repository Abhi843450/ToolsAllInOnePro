/**
 * HTTP Status Codes — handler.js
 * Delegates to Processors.http_status() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.http_status()
})();
