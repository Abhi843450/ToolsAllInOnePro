/**
 * Emoji Reference — handler.js
 * Delegates to Processors.emoji_list() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.emoji_list()
})();
