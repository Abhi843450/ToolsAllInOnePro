/**
 * Base64 Encoder/Decoder — handler.js
 * Delegates to Processors.base64_encode() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.base64_encode()
})();
