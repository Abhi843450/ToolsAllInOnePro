/**
 * W3C HTML Validator — handler.js
 * Delegates to Processors.w3c_validator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.w3c_validator()
})();
