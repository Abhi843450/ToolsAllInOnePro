/**
 * Text Difference Checker — handler.js
 * Delegates to Processors.text_diff() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.text_diff()
})();
