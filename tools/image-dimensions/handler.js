/**
 * Image Dimension Calculator — handler.js
 * Delegates to Processors.image_dimensions() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.image_dimensions()
})();
