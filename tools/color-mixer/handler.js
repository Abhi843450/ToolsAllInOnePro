/**
 * Color Mixer — handler.js
 * Delegates to Processors.color_mixer() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.color_mixer()
})();
