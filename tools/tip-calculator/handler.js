/**
 * Tip Calculator — handler.js
 * Delegates to Processors.tip_calculator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.tip_calculator()
})();
