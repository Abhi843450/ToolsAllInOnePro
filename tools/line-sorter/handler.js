/**
 * Line Sorter — handler.js
 * Delegates to Processors.line_sorter() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.line_sorter()
})();
