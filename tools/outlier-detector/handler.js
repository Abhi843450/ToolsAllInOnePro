/**
 * Outlier Detector — handler.js
 * Delegates to Processors.outlier_detector() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.outlier_detector()
})();
