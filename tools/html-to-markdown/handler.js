/**
 * HTML to Markdown — handler.js
 * Delegates to Processors.html_to_markdown() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.html_to_markdown()
})();
