/**
 * Octal Encoding — handler.js
 * Delegates to Processors.octal_encode_tool() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.octal_encode_tool()
})();
