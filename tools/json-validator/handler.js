/**
 * JSON Validator — handler.js
 * Delegates to Processors.json_validator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.json_validator()
})();
