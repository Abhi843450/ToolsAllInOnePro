/**
 * JWT Token Builder — handler.js
 * Delegates to Processors.jwt_builder() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.jwt_builder()
})();
