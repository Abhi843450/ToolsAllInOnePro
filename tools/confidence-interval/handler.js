/**
 * Confidence Interval Calculator — handler.js
 * Delegates to Processors.confidence_interval() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.confidence_interval()
})();
