/**
 * SVG to CSS Background — handler.js
 * Delegates to Processors.svg_to_css() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.svg_to_css()
})();
