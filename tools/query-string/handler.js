/**
 * Query String Builder — handler.js
 * Delegates to Processors.query_string() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.query_string()
})();
