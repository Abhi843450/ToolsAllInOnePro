/**
 * Hash Checker — handler.js
 * Delegates to Processors.hash_checker() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.hash_checker()
})();
