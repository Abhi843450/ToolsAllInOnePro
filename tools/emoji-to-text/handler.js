/**
 * Emoji to Text Description — handler.js
 * Delegates to Processors.emoji_to_text() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.emoji_to_text()
})();
