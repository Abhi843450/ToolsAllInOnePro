/**
 * XML to YAML Converter — handler.js
 * Delegates to Processors.xml_to_yaml() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.xml_to_yaml()
})();
