/**
 * Color to Tailwind Config — handler.js
 * Delegates to Processors.color_to_tailwind() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.color_to_tailwind()
})();
