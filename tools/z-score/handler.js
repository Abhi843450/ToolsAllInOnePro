/**
 * Z-Score Calculator — handler.js
 * Delegates to Processors.z_score() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.z_score()
})();
