/**
 * JSON to TOML Converter — handler.js
 * Delegates to Processors.json_to_toml() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.json_to_toml()
})();
