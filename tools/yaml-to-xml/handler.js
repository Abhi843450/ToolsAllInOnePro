/**
 * YAML to XML Converter — handler.js
 * Delegates to Processors.yaml_to_xml() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.yaml_to_xml()
})();
