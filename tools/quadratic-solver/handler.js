/**
 * Quadratic Equation Solver — handler.js
 * Delegates to Processors.quadratic_solver() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.quadratic_solver()
})();
