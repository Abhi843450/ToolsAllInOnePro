/**
 * Remove Duplicate Lines — handler.js
 * Delegates to Processors.deduplicate_lines() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.deduplicate_lines()
})();
