/**
 * Combinations & Permutations — handler.js
 * Delegates to Processors.combinatorics() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.combinatorics()
})();
