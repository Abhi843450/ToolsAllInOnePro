/**
 * Sample Size Calculator — handler.js
 * Delegates to Processors.sample_size() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.sample_size()
})();
