/**
 * Binary Encoder/Decoder — handler.js
 * Delegates to Processors.binary_encoder() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.binary_encoder()
})();
