/**
 * HTTP Header Viewer — handler.js
 * Delegates to Processors.http_header_viewer() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.http_header_viewer()
})();
