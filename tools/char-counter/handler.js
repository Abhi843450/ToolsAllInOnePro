/**
 * Character Counter — handler.js
 * Delegates to Processors.char_counter() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.char_counter()
})();
