/**
 * Hexadecimal Calculator — handler.js
 * Delegates to Processors.hex_calculator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.hex_calculator()
})();
