/**
 * CSS Border Radius Generator — handler.js
 * Delegates to Processors.border_radius() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.border_radius()
})();
