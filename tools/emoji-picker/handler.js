/**
 * Emoji Converter — handler.js
 * Delegates to Processors.emoji_picker() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.emoji_picker()
})();
