/**
 * JWT Token Decoder — handler.js
 * Delegates to Processors.jwt_decoder() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.jwt_decoder()
})();
