/**
 * T-Test Calculator — handler.js
 * Delegates to Processors.t_test() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.t_test()
})();
