/**
 * Date Format Reference — handler.js
 * Delegates to Processors.date_format_ref() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.date_format_ref()
})();
