/**
 * Docker Commands Reference — handler.js
 * Delegates to Processors.docker_cheatsheet() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.docker_cheatsheet()
})();
