/**
 * Markdown Cheat Sheet — handler.js
 * Delegates to Processors.markdown_cheatsheet() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.markdown_cheatsheet()
})();
