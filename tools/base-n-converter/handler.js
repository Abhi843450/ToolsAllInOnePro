/**
 * Base-N Converter — handler.js
 * Delegates to Processors.base_n_converter() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.base_n_converter()
})();
