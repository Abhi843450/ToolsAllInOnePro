/**
 * Text to ASCII Art — handler.js
 * Delegates to Processors.ascii_art() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.ascii_art()
})();
