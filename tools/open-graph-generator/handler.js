/**
 * Open Graph Generator — handler.js
 * Delegates to Processors.open_graph_generator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.open_graph_generator()
})();
