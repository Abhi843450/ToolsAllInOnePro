/**
 * Temperature Converter — handler.js
 * Delegates to Processors.temperature_converter() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.temperature_converter()
})();
