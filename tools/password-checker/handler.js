/**
 * Password Quality Analyzer — handler.js
 * Delegates to Processors.password_checker() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.password_checker()
})();
