/**
 * Regex Tester — handler.js
 * Delegates to Processors.regex_tester() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.regex_tester()
})();
