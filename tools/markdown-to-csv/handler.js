/**
 * Markdown Table to CSV — handler.js
 * Delegates to Processors.markdown_to_csv() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.markdown_to_csv()
})();
