/**
 * CORS Header Generator — handler.js
 * Delegates to Processors.cors_generator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.cors_generator()
})();
