/**
 * Reading Time Calculator — handler.js
 * Delegates to Processors.reading_time() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.reading_time()
})();
