/**
 * Chi-Square Calculator — handler.js
 * Delegates to Processors.chi_square() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.chi_square()
})();
