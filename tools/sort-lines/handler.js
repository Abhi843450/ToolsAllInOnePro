/**
 * Sort Lines — handler.js
 * Delegates to Processors.sort_lines() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.sort_lines()
})();
