/**
 * Image Metadata Viewer — handler.js
 * Delegates to Processors.image_metadata() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.image_metadata()
})();
