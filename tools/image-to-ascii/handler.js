/**
 * Image to ASCII Art — handler.js
 * Delegates to Processors.image_to_ascii() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.image_to_ascii()
})();
