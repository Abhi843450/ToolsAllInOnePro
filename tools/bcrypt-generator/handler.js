/**
 * Bcrypt Hash Generator — handler.js
 * Delegates to Processors.bcrypt_generator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.bcrypt_generator()
})();
