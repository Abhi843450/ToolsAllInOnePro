/**
 * Color Palette Generator — handler.js
 * Delegates to Processors.color_palette() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.color_palette()
})();
