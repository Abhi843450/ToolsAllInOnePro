/**
 * Palindrome Checker — handler.js
 * Delegates to Processors.palindrome_checker() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.palindrome_checker()
})();
