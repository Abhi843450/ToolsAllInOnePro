/**
 * Markdown to HTML — handler.js
 * Delegates to Processors.markdown_to_html() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.markdown_to_html()
})();
