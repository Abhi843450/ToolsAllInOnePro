/**
 * Random Unit of Measurement — handler.js
 * Delegates to Processors.random_unit() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.random_unit()
})();
