/**
 * Words to Number — handler.js
 * Delegates to Processors.words_to_number() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.words_to_number()
})();
