/**
 * Timezone List — handler.js
 * Delegates to Processors.timezone_list() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.timezone_list()
})();
