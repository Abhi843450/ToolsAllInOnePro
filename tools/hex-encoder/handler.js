/**
 * Hex Encoder/Decoder — handler.js
 * Delegates to Processors.hex_encoder() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.hex_encoder()
})();
