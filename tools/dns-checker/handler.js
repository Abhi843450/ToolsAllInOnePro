/**
 * DNS Lookup Tool — handler.js
 * Delegates to Processors.dns_checker() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.dns_checker()
})();
