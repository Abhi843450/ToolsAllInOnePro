/**
 * Atbash Cipher — handler.js
 * Delegates to Processors.atbash_cipher() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.atbash_cipher()
})();
