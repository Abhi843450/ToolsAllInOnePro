/**
 * HTML Encoder/Decoder — handler.js
 * Delegates to Processors.html_encoder() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.html_encoder()
})();
