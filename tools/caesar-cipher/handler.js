/**
 * Caesar Cipher — handler.js
 * Delegates to Processors.caesar_cipher() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.caesar_cipher()
})();
