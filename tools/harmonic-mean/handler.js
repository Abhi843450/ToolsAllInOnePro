/**
 * Harmonic Mean Calculator — handler.js
 * Delegates to Processors.harmonic_mean() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.harmonic_mean()
})();
