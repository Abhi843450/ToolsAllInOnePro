/**
 * Morse Code Encoder — handler.js
 * Delegates to Processors.morse_encoder() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.morse_encoder()
})();
