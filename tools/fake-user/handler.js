/**
 * Fake User Profile Generator — handler.js
 * Delegates to Processors.fake_user() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.fake_user()
})();
