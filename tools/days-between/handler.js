/**
 * Days Between Dates — handler.js
 * Delegates to Processors.days_between() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.days_between()
})();
