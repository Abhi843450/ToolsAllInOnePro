/**
 * Image to Base64 — handler.js
 * Delegates to Processors.image_to_base64() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.image_to_base64()
})();
