/**
 * Electricity Cost Calculator — handler.js
 * Delegates to Processors.electricity_calculator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.electricity_calculator()
})();
