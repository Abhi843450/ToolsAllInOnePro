/**
 * Histogram Data Generator — handler.js
 * Delegates to Processors.histogram_data() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.histogram_data()
})();
