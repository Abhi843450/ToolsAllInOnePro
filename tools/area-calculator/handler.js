/**
 * Area Calculator — handler.js
 * Delegates to Processors.area_calculator() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.area_calculator()
})();
