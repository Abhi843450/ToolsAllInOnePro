/**
 * Brainfuck Interpreter — handler.js
 * Delegates to Processors.brainfuck() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.brainfuck()
})();
