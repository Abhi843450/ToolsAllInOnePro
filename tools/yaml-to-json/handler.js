/**
 * YAML to JSON Converter — handler.js
 * Delegates to Processors.yaml_to_json() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.yaml_to_json()
})();
