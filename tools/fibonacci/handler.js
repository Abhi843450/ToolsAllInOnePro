/**
 * Fibonacci Calculator — handler.js
 * Delegates to Processors.fibonacci() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.fibonacci()
})();
