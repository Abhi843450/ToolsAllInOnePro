/**
 * Text Reverser — handler.js
 * Delegates to Processors.reverse_text_tool() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.reverse_text_tool()
})();
