/**
 * Markdown Link Generator — handler.js
 * Delegates to Processors.markdown_link() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.markdown_link()
})();
