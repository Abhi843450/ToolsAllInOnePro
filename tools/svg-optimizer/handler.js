/**
 * SVG Optimizer — handler.js
 * Delegates to Processors.svg_optimizer() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.svg_optimizer()
})();
