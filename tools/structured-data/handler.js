/**
 * Structured Data Generator — handler.js
 * Delegates to Processors.structured_data() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.structured_data()
})();
