/**
 * HTML to Text — handler.js
 * Delegates to Processors.html_to_text() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.html_to_text()
})();
