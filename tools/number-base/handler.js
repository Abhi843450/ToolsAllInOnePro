/**
 * Number Base Converter — handler.js
 * Delegates to Processors.number_base() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.number_base()
})();
