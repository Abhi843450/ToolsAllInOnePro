/**
 * Safe Web Colors — handler.js
 * Delegates to Processors.safe_colors() in processors.js
 */
(function() {
  // Register as a text-processing tool
  // tool.js will auto-invoke Processors.safe_colors()
})();
